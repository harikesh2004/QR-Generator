<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR Code Generator</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(to right, #4facfe, #00f2fe);
            color: #333;
        }
        
        header {
            display: flex;
            justify-content: space-between;
            padding: 20px;
            background: rgba(0, 0, 0, 0.7);
            color: white;
        }
        .title{
            display: flex;
        }
        .logo{
            display: flex;
        }
        .logo img {
            width: 50px;
            height: 50px;
        }
        
        .auth-buttons {
            display: flex;
            gap: 10px;
            padding-bottom: 10px;
        }
        
        .auth-buttons .btn {
            color: white;
            background: #4facfe;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            transition: background 0.3s;
            
        }
        
        .auth-buttons .btn:hover {
            background: #00f2fe;
        }
        .auth-buttons .user{
            font-size: medium;
            text-align: center;
            font-weight: bold;
            margin:10px 10px 0px 0px;
        }
        main {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 90vh;
            padding: 20px;
        }
        
        .hero-section {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 50px;
            animation: fadeIn 2s;
        }
        
        .hero-content {
            max-width: 600px;
        }
        
        .hero-content h1 {
            font-size: 3rem;
            margin-bottom: 20px;
            color: white;
        }
        
        .hero-content p {
            font-size: 1.2rem;
            margin-bottom: 30px;
            color: white;
        }
        
        .cta-button {
            display: inline-block;
            padding: 15px 30px;
            font-size: 1.2rem;
            background: #4facfe;
            color: white;
            border-radius: 5px;
            text-decoration: none;
            transition: background 0.3s;
        }
        
        .cta-button:hover {
            background: #00f2fe;
        }
        
        
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
        
    </style>
</head>
<body>
    <header>
        
        <div class="logo">
            <img src="/images/logo.png" alt="Logo">
            <h1 style="margin: 0px 0px 10px 20px;">QR CODE GENERATOR</h1>
        </div>
        
            
        
        <div class="auth-buttons">
            <% if (username) { %>
                <span class="user">Welcome, <%= username %>!</span>
                <a href="/generate-qr?username=<%= username %>" class="btn">Generate QR Code</a>
                <a href="/logout" class="btn">Log Out</a>
            <% } else { %>
                <a href="/login" class="btn">Log In</a>
                <a href="/signup" class="btn">Sign Up</a>
            <% } %>
        </div>
    </header>
    <main>
        <div class="hero-section">
            <div class="hero-content">
                <h1>Generate Your QR Code</h1>
                <p>Enter any text or URL below to generate a custom QR code that you can use for your personal or business needs.</p>
                
            </div>
            <div class="auth-buttons">
                <a href="/generate-qr?username=<%= username %>" class="btn">Generate QR Code</a>
            </div>
        </div>
    </main>
</body>
</html>
